# Always resolve tarballs from the public registry; never the corporate
# Nexus mirror (it has served corrupted tarballs, breaking CI installs).
registry=https://registry.npmjs.org/
# Legacy peer deps compatibility setting (retained for React 19 / Expo 57 tooling constraints).
legacy-peer-deps=true
